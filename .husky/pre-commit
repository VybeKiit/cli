# Lenient on purpose: format + safe lint fixes only — never blocks on a failing test.
# The real gate (tests + typecheck + build) lives in CI. This must stay friendly enough
# to be inherited by a non-coder's scaffolded repo. See AGENTS.md.
pnpm biome format --write --no-errors-on-unmatched .
