/** Placeholder — run `vybekiit-automate extension import --json` after CWS sign-in to populate from live store. */
// Open record so this generated stub never references a private maintainer package
// (@vybekiit/browser-automation is not published — ADR-0025); the import step fills the real shape.
export default {} as Record<string, unknown>;
