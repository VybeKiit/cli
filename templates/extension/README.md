# templates/extension — WXT (ships in v3)

Placeholder. The browser-extension template ships in **v3** (see `CONTEXT.md` → Build order).

Planned: **WXT** + **shadcn/ui** (shared with the web template), the same `@vybekiit/*` packages and
the same buyer-facing agent layer (`AGENTS.md`, `language.md`, `.vybekiit/skills`). Extensions
request least-privilege host permissions by default.

Not built yet — intentionally empty so the planned structure is legible without shipping dead code.
