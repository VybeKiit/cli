# templates/extension — WXT (ships in v3)

Browser-extension template: **WXT** + **shadcn/ui** (shared with web), `@vybekiit/*` packages, and
the buyer-facing agent layer (`AGENTS.md`, `language.md`, `.vybekiit/skills/`, platform wrappers).

**Agent layer:** 7 buyer goal skills + `chrome-extension-vybekiit.md` platform wrapper (Chrome API
docs as source of truth). Application scaffold (WXT app code) lands with the full v3 template build.
