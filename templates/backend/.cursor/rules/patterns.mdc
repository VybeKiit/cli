---
description: Code hygiene checklist (not SSOT)
alwaysApply: true
---

- Use `@vybekiit/*` packages from `src/services/`.
- Read `AGENTS.md` and `.vybekiit/agent/session-bootstrap.md` at the start of every task.
- MVC: routes → controllers → services.
- Validate request bodies with zod at route boundary.
