# templates/mobile — Expo (ships in v2)

Placeholder. The mobile template ships in **v2** (see `CONTEXT.md` → Build order).

Planned: **Expo** + **NativeWind / react-native-reusables** (shadcn-parity UI), the same
`@vybekiit/*` packages (payments, accounts, data) and the same buyer-facing agent layer
(`AGENTS.md`, `language.md`, `.vybekiit/skills`), with deploy handled by the author's `launch` CLI.

Not built yet — intentionally empty so the planned structure is legible without shipping dead code.
