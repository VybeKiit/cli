import { MobileSaasRoute } from '@/components/mobileSaasRoute';

/** Render the user settings route. */
const SettingsScreen = () => <MobileSaasRoute screen="settings" />;

export default SettingsScreen;
