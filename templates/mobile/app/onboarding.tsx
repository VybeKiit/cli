import { MobileSaasRoute } from '@/components/mobileSaasRoute';

/** Render the post-signup onboarding route. */
const OnboardingScreen = () => <MobileSaasRoute screen="onboarding" />;

export default OnboardingScreen;
