---
description: VybeKiit agent layer — read AGENTS.md
alwaysApply: true
---

# VybeKiit agent layer

Read and follow `./AGENTS.md` as the single source of truth for this project. At task start,
read `.vybekiit/agent/session-bootstrap.md` for the read order. It defines the contract
(Decide + Guide), the plain-language rules in `language.md`, and how to pick the right skill
in `.vybekiit/skills/`.

Treat `.env` as redacted — never reproduce secret values from it in chat or commits.

This file is only a pointer — do not duplicate any rules here.
