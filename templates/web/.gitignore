# dependencies
node_modules/

# next.js build output
.next/
out/

# typescript (next-env.d.ts is generated by `next build`)
next-env.d.ts
*.tsbuildinfo

# turbo
.turbo/

# env / secrets — only .env.example is committed
.env
.env.*
!.env.example

# os
.DS_Store

# local agent state (planning intro seen, etc.)
.vybekiit/state/

# maintainer-only script scratch — never shipped to buyers (ADR-0027)
scripts/dev/
