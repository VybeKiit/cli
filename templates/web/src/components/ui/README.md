# components/ui/

Core shadcn/ui primitives have moved to `@vybekiit/ui` (packages/ui/).

Import shared primitives from the package:
```typescript
import { Button, Input, Sheet } from '@vybekiit/ui/button';
```

This directory is reserved for template-local custom UI components that
are NOT part of the shared library.
