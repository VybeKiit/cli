# dependencies
node_modules/
.pnpm-store/

# build output
dist/
.next/
.turbo/
out/
*.tsbuildinfo
next-env.d.ts

# env / secrets — NEVER commit real values (only .env.example is tracked)
.env
.env.*
!.env.example

# logs / os
*.log
.DS_Store
coverage/

# local / private (operator notes — not for the repo)
docs/checklist.md
.bridge/
downloads/

# session scratch — agent worktrees + local harness settings (never tracked)
.claude/
