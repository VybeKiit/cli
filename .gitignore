# dependencies
node_modules/
.pnpm-store/

# build output
dist/
.next/
.turbo/
out/
*.tsbuildinfo
next-env.d.ts

# env / secrets — NEVER commit real values (only .env.example is tracked)
.env
.env.*
!.env.example

# logs / os
*.log
.DS_Store
coverage/

# local / private (operator notes — not for the repo)
docs/checklist.md
.bridge/
downloads/
# Per-developer Claude Code local overrides — never committed, so Biome's
# useIgnoreFile keeps them out of the lint gate too (they exist only locally).
.claude/settings.local.json
