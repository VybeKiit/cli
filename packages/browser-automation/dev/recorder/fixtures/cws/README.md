# CWS probe fixtures

Maintainer-only DOM candidate snapshots from an earlier probing workflow. **Not runtime input** — the live pipeline uses Inspector drafts (`.cws-selectors.draft.txt`) → `selectors.generated.ts`.

Kept as reference when validating selector field coverage.
