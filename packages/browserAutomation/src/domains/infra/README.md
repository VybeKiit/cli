# Infrastructure providers

Cloudflare / Wrangler deployment is **skill-driven** — no Playwright dashboard automation in v1.

Use `doctor`, `deploy`, and `wrangler` platform skills for buyer projects.

Do not add dashboard DOM automation here unless an ADR amends this scope.
