{
  "name": "@vybekiit/email-worker",
  "version": "0.3.0",
  "private": true,
  "description": "Cloudflare Worker entry for @vybekiit/email — deploy separately from the npm package.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250523.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.105.0"
  }
}
