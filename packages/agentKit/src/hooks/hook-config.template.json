{
  "$schema": "https://vybekiit.com/schemas/hook-config.json",
  "level": "standard",
  "disabled": [],
  "notes": {
    "levels": {
      "minimal": "P1-P2 only (safety-critical). Fastest, least context.",
      "strict": "P1-P5 (safety + quality). Good for established codebases.",
      "standard": "P1-P7 (default). Balance of safety, quality, and DX nudges.",
      "relaxed": "P1-P9 (everything). For greenfield projects."
    },
    "disable_example": ["check-i18n-hardcoded", "enforce-barrel-exports"],
    "env_override": "Set VYBEKIIT_HOOK_LEVEL=minimal to override per-session"
  }
}
